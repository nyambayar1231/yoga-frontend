# Base URL the frontend calls. Leave as /api to use the Vite dev proxy.
VITE_API_BASE_URL=/api

# Where the Vite dev proxy forwards /api requests (yoga-cms-backend).
BACKEND_URL=http://localhost:3000
